{
  "enabled": true,
  "name": "스토리북 자동 생성",
  "description": "새 컴포넌트 생성 시 자동으로 Storybook 스토리 파일을 생성합니다",
  "version": "1",
  "when": {
    "type": "fileCreated",
    "patterns": [
      "blog/src/**/*.story.tsx"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "새로운 React 컴포넌트 파일이 생성되었습니다. 다음 작업을 수행해주세요:\n\n## 제외 조건 확인\n먼저 생성된 파일이 다음 패턴에 해당하는지 확인하고, 해당하면 스토리 생성을 건너뜁니다:\n- `*.test.tsx` - 테스트 파일\n- `*.stories.tsx` - 스토리 파일\n- `page.tsx` - Next.js 페이지 파일\n- `layout.tsx` - Next.js 레이아웃 파일\n\n## 1단계: 컴포넌트 분석\n- 컴포넌트의 Props 인터페이스 확인\n- Export 방식 확인 (default vs named)\n- 컴포넌트 타입 파악 (UI 컴포넌트, Feature 컴포넌트 등)\n\n## 2단계: Storybook 스토리 생성\n같은 디렉토리에 `{파일명}.stories.tsx` 파일을 다음 템플릿 기반으로 생성:\n\n```typescript\nimport type { Meta, StoryObj } from '@storybook/react';\nimport { {컴포넌트명} } from './{파일명}';\n\nconst meta = {\n  title: '{레이어}/{컴포넌트명}',\n  component: {컴포넌트명},\n  tags: ['autodocs'],\n  argTypes: {\n    // Props 기반 argTypes 자동 생성\n  },\n} satisfies Meta<typeof {컴포넌트명}>;\n\nexport default meta;\ntype Story = StoryObj<typeof meta>;\n\nexport const Default: Story = {\n  args: {\n    // 기본 props 값\n  },\n};\n\n// 추가 스토리 (필요시)\n```\n\n## 3단계: title 경로 규칙\n파일 경로에 따라 Storybook title을 결정합니다:\n- `src/shared/ui/` → `UI/{컴포넌트명}`\n- `src/shared/components/` → `UI/{컴포넌트명}`\n- `src/features/*/ui/` → `Features/{Feature명}/{컴포넌트명}`\n- `src/widgets/` → `Widgets/{컴포넌트명}`\n- `src/entities/` → `Entities/{컴포넌트명}`\n\n## 4단계: argTypes 설정\nProps 타입에 따라 적절한 control을 설정합니다:\n- Props가 없는 경우: 기본 스토리만 생성\n- Boolean props: `control: 'boolean'`\n- Enum/Union props: `control: 'select', options: [...]`\n- 함수 props: `action: '{함수명}'`\n- string props: `control: 'text'`\n- number props: `control: 'number'`\n\n## 5단계: 스토리 변형 생성\nProps의 주요 변형에 대해 추가 스토리를 생성합니다:\n- variant props가 있으면 각 variant별 스토리 생성\n- size props가 있으면 각 size별 스토리 생성\n- disabled 상태가 있으면 Disabled 스토리 생성\n\n## 6단계: 생성 완료 후 출력\n```\n✅ Storybook 스토리 생성 완료\n\n📁 생성된 파일:\n  {파일경로}.stories.tsx\n\n📖 스토리 목록:\n  - Default\n  - {추가 스토리들...}\n\n💡 Storybook 실행:\n  pnpm storybook\n```\n\n## 중요 사항\n- 기존 스토리 파일이 있으면 생성하지 않음\n- `page.tsx`, `layout.tsx` 같은 Next.js 파일은 제외\n- 테스트 파일(`*.test.tsx`)은 제외\n- 스토리 파일(`*.stories.tsx`)은 제외"
  },
  "workspaceFolderName": "blog",
  "shortName": "auto-storybook"
}